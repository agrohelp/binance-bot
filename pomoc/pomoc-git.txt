-configuracja globalna git
git config --global user.name "Karol"
git config --global user.email "szew.karol@gmail.com"
--
-Inicjacja git w projekcie
cd C:/binance_bot
git init
--
- pierwszy commit
git add .
git commit -m "Początkowy szkielet bota Binance"
--
- sprawdz czy działa
git config --list

--GitHub
-repozytorjum
https://github.com/agrohelp/binance-bot

--polącz lokalne repo z GitHub
git remote add origin https://github.com/agrohelp/binance-bot.git
- sprawdz czy remote (zdalny) działa 
remote -v

-git branch → operacje na gałęziach -M → move/rename z wymuszeniem -bot → nowa nazwa gałęzi
git branch -M bot

-wyslij do GitHub
git push -u origin bot

--Pierwszy commit po zmianach
git add .
git commit -m "Dodano obsługę .env i bezpieczną konfigurację"
git push

--Po zmianach
--sprawdz zmiany w plikach
git status
-- zmiany są więc:
-1)dodaj wszystkie zmiany
git add .
-2) Zrób commit
git commit -m "Kod dostosowany do klucza 'tylko odczyt'"

3) Wyślij zmiany na GitHub
git push lub
git push origin master lub

git tag v1.0-stable
git push origin v1.0-stable

-- commit historia opis data i godzina commita
git log
git log --oneline  - wyswietla wszystkie commity i ich ID
git log --oneline --graph
git log -n 5   - ostatnie 5
git log -- bot.py  - dla jednego pliku
git log --author="Karol"   - autora
git log --grep="read-only"   - zawierajace słowo
git log -p   - pokazuje zmiany - bardzo obszerny nie stosuj
git log --name-only  - tylko zmienione pliki
git log --stat   - statystyki zmian










